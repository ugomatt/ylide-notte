/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['lh3.googleusercontent.com', 'ylide.io', 'docs.metamask.io', 'walletconnect.com'],
  },
}

module.exports = nextConfig
